# PromptLab Configuration
# Copy this file to .env — the app will fill in FERNET_KEY on first run.
# NEVER commit your .env file.

# Master encryption key (auto-generated on first startup)
FERNET_KEY=

# Optional overrides
# APP_HOST=127.0.0.1
# APP_PORT=8000
